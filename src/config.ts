import type { AtlasDatabaseConfig } from "./AtlasProvider.js";

export function defineConfig(config: AtlasDatabaseConfig): AtlasDatabaseConfig {
	return config;
}

export type { AtlasDatabaseConfig };
