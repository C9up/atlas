[package]
name = "atlas-query"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "SQL query compiler for the Ream framework"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
